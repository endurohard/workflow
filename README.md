# Система управления выездным сервисом

Комплексная платформа для управления сервисом по ремонту и обслуживанию техники, построенная на микросервисной архитектуре с использованием Kong API Gateway.

## 🏗️ Архитектура

Проект построен на микросервисной архитектуре с следующими компонентами:

### Сервисы

- **Kong API Gateway** - точка входа для всех API запросов
- **Auth Service** (порт 3001) - аутентификация и авторизация
- **Users Service** (порт 3002) - управление пользователями и техниками
- **Tasks Service** (порт 3003) - управление заявками и заказами
- **Schedule Service** (порт 3004) - планирование и календарь
- **Notifications Service** (порт 3005) - система уведомлений
- **Reports Service** (порт 3006) - отчеты и аналитика
- **Frontend** (порт 3000) - веб-интерфейс на React

### База данных

- **PostgreSQL** - основная база данных
- **Redis** - кеширование и real-time функциональность

## 🚀 Быстрый старт

### Требования

- Docker >= 20.10
- Docker Compose >= 2.0

### Запуск проекта

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd workflow
```

2. Запустите все сервисы:
```bash
docker-compose up -d
```

3. Проверьте статус сервисов:
```bash
docker-compose ps
```

4. Откройте приложение в браузере:
```
http://localhost:3000
```

### API Gateway

Kong API Gateway доступен по адресу:
- Proxy: http://localhost:8000
- Admin API: http://localhost:8001

Все API запросы должны идти через Kong:
```bash
# Пример: вход в систему
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "admin@example.com", "password": "password123"}'
```

## 📚 API Документация

### Auth Service

- `POST /api/auth/register` - регистрация нового пользователя
- `POST /api/auth/login` - вход в систему
- `POST /api/auth/refresh` - обновление токена
- `POST /api/auth/logout` - выход из системы
- `POST /api/auth/forgot-password` - восстановление пароля
- `POST /api/auth/reset-password` - сброс пароля

### Users Service

- `GET /api/users` - список пользователей
- `GET /api/users/:id` - информация о пользователе
- `POST /api/users` - создание пользователя
- `PUT /api/users/:id` - обновление пользователя
- `DELETE /api/users/:id` - удаление пользователя
- `GET /api/users/technicians` - список техников
- `GET /api/users/technicians/:id/schedule` - расписание техника

### Tasks Service

- `GET /api/tasks` - список задач
- `GET /api/tasks/:id` - информация о задаче
- `POST /api/tasks` - создание задачи
- `PUT /api/tasks/:id` - обновление задачи
- `PATCH /api/tasks/:id/status` - обновление статуса
- `DELETE /api/tasks/:id` - удаление задачи
- `GET /api/orders` - список заказов
- `POST /api/orders` - создание заказа

### Schedule Service

- `GET /api/schedule` - общее расписание
- `GET /api/schedule/technician/:id` - расписание техника
- `POST /api/schedule` - создание записи в расписании
- `GET /api/calendar` - календарь событий

### Notifications Service

- `GET /api/notifications` - список уведомлений
- `POST /api/notifications` - создание уведомления
- `PATCH /api/notifications/:id/read` - отметить как прочитанное

### Reports Service

- `GET /api/reports/tasks` - отчет по задачам
- `GET /api/reports/technicians` - отчет по техникам
- `GET /api/analytics/dashboard` - данные для дашборда

## 🗄️ База данных

### Схема

Основные таблицы:

- `users` - пользователи системы
- `technicians` - техники (расширенная информация)
- `clients` - клиенты
- `orders` - заявки/заказы
- `tasks` - задачи (связь заказов с техниками)
- `schedules` - расписание техников
- `notifications` - уведомления
- `task_comments` - комментарии к задачам
- `order_status_history` - история статусов
- `reports` - отчеты

### Доступ к базе данных

```bash
docker-compose exec postgres psql -U serviceuser -d service_manager
```

## 🔧 Разработка

### Структура проекта

```
workflow/
├── services/
│   ├── auth/           # Auth Service
│   ├── users/          # Users Service
│   ├── tasks/          # Tasks Service
│   ├── schedule/       # Schedule Service
│   ├── notifications/  # Notifications Service
│   └── reports/        # Reports Service
├── frontend/           # React Frontend
├── database/
│   └── init/          # SQL скрипты инициализации
├── kong.yaml          # Kong конфигурация
└── docker-compose.yml # Docker Compose конфигурация
```

### Разработка сервиса

Для разработки отдельного сервиса:

```bash
cd services/auth
npm install
npm run dev
```

### Разработка frontend

```bash
cd frontend
npm install
npm run dev
```

## 🐳 Docker команды

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Просмотр логов
docker-compose logs -f [service-name]

# Пересборка сервиса
docker-compose build [service-name]

# Перезапуск сервиса
docker-compose restart [service-name]

# Удаление всех данных (volumes)
docker-compose down -v
```

## 📊 Мониторинг

### Health checks

Каждый сервис имеет endpoint для проверки здоровья:

```bash
# Auth Service
curl http://localhost:8000/api/auth/../health

# Users Service
curl http://localhost:8000/api/users/../health

# И т.д. для остальных сервисов
```

### Логи

Просмотр логов Kong:

```bash
docker-compose exec kong tail -f /var/log/kong/access.log
```

## 🔐 Безопасность

- Все пароли хешируются с использованием bcrypt
- JWT токены для аутентификации
- HTTPS рекомендуется для production
- Kong плагины для rate limiting и CORS
- Переменные окружения для чувствительных данных

## 🚀 Production

Для production deployment:

1. Измените все пароли в `docker-compose.yml`
2. Настройте SSL сертификаты для Kong
3. Используйте внешние базы данных
4. Настройте backup стратегию
5. Включите monitoring и logging
6. Настройте автомасштабирование

## 📝 Тестовые данные

После первого запуска в БД будут созданы тестовые пользователи:

- **Админ**: admin@example.com / password123
- **Диспетчер**: dispatcher@example.com / password123
- **Техник 1**: tech1@example.com / password123
- **Техник 2**: tech2@example.com / password123
- **Клиент 1**: client1@example.com / password123
- **Клиент 2**: client2@example.com / password123

## 🤝 Вклад в проект

1. Fork проект
2. Создайте feature branch
3. Commit изменения
4. Push в branch
5. Создайте Pull Request

## 📄 Лицензия

MIT License

## 👥 Контакты

Для вопросов и предложений создавайте issue в репозитории.
