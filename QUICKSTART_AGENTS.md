# 🚀 Быстрый старт с Agent Orchestrator

## За 5 минут до запуска агентов!

### 1️⃣ Авторизуйтесь в GitHub (1 мин)

```bash
gh auth login
```

Выберите:
- GitHub.com
- HTTPS
- Yes (authenticate Git with GitHub credentials)
- Login with a web browser (или Paste an authentication token)

### 2️⃣ Создайте все Issues (1 мин)

```bash
./.github/create-all-issues.sh
```

Это создаст **24 issue** в вашем репозитории.

### 3️⃣ Проверьте Issues (30 сек)

```bash
gh issue list --repo endurohard/workflow
```

### 4️⃣ Запустите Dashboard (30 сек)

```bash
PORT=3001 ao start
```

Откройте http://localhost:3001 в браузере.

### 5️⃣ Запустите первую волну агентов! (2 мин)

```bash
# Запуск 5 агентов для critical infrastructure
ao batch-spawn workflow AUTH-001 USERS-001 DB-001 REDIS-001 REDIS-002
```

---

## 🎯 Что будет происходить?

1. **Создадутся worktrees** для каждой задачи
2. **Запустятся tmux сессии** с агентами
3. **Агенты начнут работу**:
   - Читать описание issue
   - Анализировать код
   - Писать реализацию
   - Создавать тесты
   - Делать коммиты
   - Создавать PR

4. **Вы мониторите** через Dashboard
5. **CI/CD работает** автоматически
6. **Review комментарии** обрабатываются автоматически
7. **Merge происходит** после approve + green CI

---

## 📊 Мониторинг

### В терминале

```bash
# Статус всех сессий
ao status

# Список сессий
ao session ls -p workflow

# Подключиться к агенту
ao session attach wor-1
```

### В браузере

http://localhost:3001 - Web Dashboard

---

## 🌊 Рекомендуемая последовательность волн

### Волна 1: Foundation (запустить СЕЙЧАС)

```bash
ao batch-spawn workflow AUTH-001 USERS-001 DB-001 REDIS-001 REDIS-002
```

Подождите пока все PRs не будут merged (~3-4 часа с агентами).

### Волна 2: Business Logic (запустить после Волны 1)

```bash
ao batch-spawn workflow TASKS-001 SCHEDULE-001 NOTIFY-001
```

### Волна 3: Telegram Bot (запустить после Волны 2)

```bash
ao batch-spawn workflow BOT-001 BOT-002 BOT-003 BOT-004
```

### Волна 4: Frontend (параллельно с Волной 3)

```bash
ao batch-spawn workflow FE-001 FE-002 FE-003 FE-004 FE-005
```

### Волна 5: Testing (запустить после Волны 4)

```bash
ao batch-spawn workflow REPORTS-001 TEST-001 TEST-002 DB-002
```

### Волна 6: DevOps (финальная)

```bash
ao batch-spawn workflow CI-001 TEST-003 DEPLOY-001
```

---

## 🆘 Если что-то пошло не так

### Агент застрял?

```bash
# Посмотреть логи
ao logs wor-1

# Отправить инструкцию
ao send wor-1 "Пожалуйста, исправь ошибку компиляции"

# Перезапустить
ao session kill wor-1
ao spawn workflow AUTH-001
```

### CI упал?

Агент автоматически получит логи и исправит (до 3 попыток).

### Review с комментариями?

Агент автоматически обработает и запушит фиксы.

### Нужно вмешаться руками?

```bash
# Подключиться к сессии
ao session attach wor-1

# Или работать напрямую в worktree
cd ~/.worktrees/workflow-wor-1
```

---

## 💰 Ожидаемые результаты

- **Время разработки**: 20-25 дней → ~2-3 дня с агентами
- **Количество кода**: ~50,000+ строк
- **Покрытие тестами**: 80%+
- **PRs**: 24 pull requests
- **Автоматизация**: ~90% работы

---

## ✅ Чеклист перед стартом

- [ ] GitHub CLI установлен (`gh --version`)
- [ ] Авторизация в GitHub (`gh auth status`)
- [ ] Agent Orchestrator работает (`ao status`)
- [ ] Dashboard запущен (http://localhost:3001)
- [ ] Issues созданы (`gh issue list --repo endurohard/workflow`)
- [ ] Готовы запустить агентов! 🚀

---

## 🎉 Запускайте!

```bash
# GO GO GO!
ao batch-spawn workflow AUTH-001 USERS-001 DB-001 REDIS-001 REDIS-002
```

Теперь откройте Dashboard и наблюдайте за магией! ✨

---

**Tip**: Держите Dashboard открытым в одной вкладке, терминал с `ao status` в другой, и просто наблюдайте как агенты работают!

**Pro Tip**: Можете идти пить кофе ☕ - агенты справятся сами. Вернетесь — PRs будут готовы!
